{
    "name": "frontend-red-admin",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "serve -s . -l $PORT",
        "build": "echo 'Build no requerido' && exit 0",
        "predeploy": "npm run build",
        "deploy": "echo 'Desplegado en Render'"
    },
    "dependencies": {
        "serve": "^14.2.0"
    },
    "devDependencies": {
        "eslint": "^8.56.0",
        "prettier": "^3.2.4"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}
